/**
 * Dashboard entry point — delegates to the unified backend server.
 *
 * Usage:
 *   npx ts-node scripts/dashboard.ts
 *
 * This file is kept for backwards compatibility with the documented
 * `npm run dashboard` command. All logic lives in src/server/server.ts.
 */
import "../src/server/server";
